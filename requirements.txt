# CPU-only torch — install this first with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
torch
sentence-transformers>=6.1
chromadb>=1.5
Flask>=3.1
python-pptx>=1.0
python-docx>=1.2
pymupdf>=1.28
requests>=2.34
